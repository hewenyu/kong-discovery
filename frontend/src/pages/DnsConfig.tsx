import { Card, Typography } from 'antd';

const { Title } = Typography;

const DnsConfig = () => {
  return (
    <Card>
      <Title level={3}>DNS配置</Title>
      <p>DNS配置页面 - 待实现</p>
    </Card>
  );
};

export default DnsConfig; 